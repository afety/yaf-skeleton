<?php

namespace <namespace>;

class <modelName> extends <parentClassName>
{
    protected $table = '<tableName>';

    protected $fillable = [
        <fillable>
    ];

    protected $fieldsCommon = [
        <fieldsComment>
    ];

    // Getter Func
    <getterZone>

    // Setter Func
    <setterZone>
}